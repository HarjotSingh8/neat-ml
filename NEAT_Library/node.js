class Node {
  constructor(parameters) {
    this.type = parameters.type; //big thanks to tha coding godd @sehajdeep_sandhu, hit my guy up on the insta bro big shout outs
    this.connections = [];
    this.activation = parameters.activation;
  }
}
